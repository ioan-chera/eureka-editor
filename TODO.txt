
EUREKA SHORT-TERM TODO
======================


General
-------

>>>  new Open Map dialog
    -  implement button callback

    !!!!  how to handle: Load a new edit_wad, then CANCEL


+  support resource wads
   (a) -merge option for command line [DONE!]
   (b) open them in R/O mode (like the IWAD)
>>>   (c) File / Manage Wads: a window showing iwad, pwad & resource wads,
       [probably port and mod too] and ability to change them
   (d) persist in a lump, e.g. "__EUREKA"
   (e) load definitions from mods/<base>.ugh

>>>  Preferences window
    - coding time!

>>>  UI_ChooseMap
     -  valid input on each press, only enable OK button 
        when input is valid (change text color to FL_RED if invalid)


Documentation
-------------


Coding
------


Editing
-------

>>>  'd' disconnect sectors
 
     -  fix textures when separating linedef : LD_RemoveSideDef()
     -  better DisconnectCoord
     ?  include islands (perhaps with 'D')

>>> 'm' in linedef mode : two one-side linedefs, join into one
   -  allow a shared vertex
   -  fix texturing : use LD_AddedSideDef)


Browser
-------


Renderer
--------


Game / Port Definitions
-----------------------

